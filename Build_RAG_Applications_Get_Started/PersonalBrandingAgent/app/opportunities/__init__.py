"""Opportunity-aware branding: tracked work and its coverage baseline (M1).

This package will grow the change-detection (M2) and opportunity-selection
(M3) milestones. It starts with the only thing M1 needs: loading the
declarative tracked-work configuration and seeding it into the state store,
idempotently and without touching publication history.
"""
