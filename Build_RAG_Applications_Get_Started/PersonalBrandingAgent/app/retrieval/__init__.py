"""Retrieval layer: strategies over the ingested Chroma corpus.

Every strategy returns the same RetrievalResult shape (see models.py), so
the comparison playground and evaluation can treat them uniformly.
"""
