# The key the Agent reasoner, the generator and the support judge actually
# use: a Google AI Studio key (the AIza-… family), sent to the Gemini API
# directly by the native SDK against one pinned model (config.GEMINI_MODEL_ID).
GOOGLE_API_KEY=<YOUR_GOOGLE_AI_STUDIO_KEY>
# Optional model override; the default pin is deliberate, not a placeholder.
# Never point an unattended path at a "-preview"/"-exp" model.
# GEMINI_MODEL_ID=gemini-3.6-flash
# Multi-query retrieval still goes through OpenRouter (OpenAI-compatible
# client at OPENROUTER_BASE_URL), so its value must be an OpenRouter key
# (the sk-or-v1-… family), not an OpenAI platform key (sk-proj-…). Takes
# precedence when both are set.
OPENROUTER_API_KEY=<YOUR_OPENROUTER_KEY>
# Legacy alias for the same value; OPENROUTER_API_KEY above is preferred.
OPENAI_API_KEY=<YOUR_OPENROUTER_KEY>
LINKEDIN_CLIENT_ID=<YOUR_LINKEDIN_CLIENT_ID>
LINKEDIN_CLIENT_SECRET=<YOUR_LINKEDIN_CLIENT_SECRET>

# Operational failure notification (PLAN.md Step 7). All required values must
# be set for a notification to be attempted; while any is empty the
# application still runs and still records failures, it just cannot alert.
# For Gmail: an app password, not the account password, and the sender address
# must be the account that owns it.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SENDER=<YOUR_GMAIL_ADDRESS>
SMTP_RECIPIENT=<WHERE_ALERTS_SHOULD_GO>
SMTP_USERNAME=<YOUR_GMAIL_ADDRESS>
SMTP_PASSWORD=<YOUR_GMAIL_APP_PASSWORD>
# starttls | ssl | none
SMTP_TLS=starttls
