# LangChain 1.x generation stack (installed versions verified 2026-09-08).
# Pin the core retrieval/embedding stack so a fresh install matches the
# environment this was built and tested against; minor bumps are expected
# to be compatible.
langchain>=1.3,<2
langchain-core>=1.6,<2
langchain-community>=0.4,<1
langchain-openai>=1.5,<2        # OpenAI-compatible client -> OpenRouter
langchain-chroma>=1.1,<2        # Chroma integration (used via raw collection API)
langchain-huggingface>=1.2,<2   # local MiniLM embeddings
langchain-text-splitters>=1.1,<2

chromadb>=1.5,<2                # persistent local vector store
sentence-transformers>=5.7,<6   # embeddings + cross-encoder reranker
rank_bm25>=0.2.2,<1             # BM25 lexical retrieval

pydantic>=2.13,<3
python-dotenv>=1.2,<2
requests>=2.34,<3

# Source registry (sources.yaml) — Step 2. Previously present only as a
# transitive dependency of the LangChain stack, which meant the registry would
# stop loading if that stack ever dropped it. The registry is committed
# configuration read on every synchronization, so it is pinned directly.
PyYAML>=6.0,<7

# Testing
pytest>=9.1,<10

# Application interface (NOT used yet — reserved for the Flask phase; the
# retrieval milestone runs entirely as CLIs)
Flask>=3.0
